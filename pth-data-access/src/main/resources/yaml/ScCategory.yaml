#运行AutoMapper.java自动生成xml
sc_category :
    model : com.sankuai.meituan.kuailv.ssc.model.ScCategory
    prikey : id
    columns:
        id : id
        parent_id : parentId
        name : name
        level : level
        leaf : leaf
        sort : sort
        path : path
        test : test
        leaf : leaf
        removed : removed
        ctime : ctime
        utime : utime
        creator_id : creatorId
        updater_id : updaterId
        creator_name : creatorName
        updater_name : updaterName
    insert:
        ctime : unix_timestamp()
    update:
        utime : unix_timestamp()